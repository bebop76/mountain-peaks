<%- include('partials/header') -%>

<header>
    <div class="container">

      <div class="">
        <img src="asset/mountain-logo.png" alt="">
      </div>
      <div class="headings">
        <h2>Activation</h2>
        
        <h3><%= stato %></h3>
        <% if(stato === 'Something went wrong !') { %>
          <a href="/noactive" class="btn">send activation again</a>
        <% } else {%>
          <a href="/mountains" class="btn">Go to mountains</a>
        <% } %>
      </div>
    </div>
</header>

<%- include('partials/footer') -%>